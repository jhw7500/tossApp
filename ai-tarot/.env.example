# Development uses Vite's /api proxy when this is omitted.
# Production must set an explicit HTTPS endpoint.
VITE_API_BASE_URL=

# Browser-only development identity. Never enable in a production build.
VITE_LOCAL_MOCK_AUTH=false
